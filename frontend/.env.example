# ── Development ───────────────────────────────────────────────────────────────
# Leave VITE_API_BASE_URL empty in development.
# Vite proxies /api and /auth to the backend automatically (see vite.config.ts),
# so the frontend and backend appear to be on the same origin and no CORS applies.
VITE_API_BASE_URL=

# ── Production ────────────────────────────────────────────────────────────────
# Set this to the full backend origin when deploying.
# The built JS runs in the browser, not through the Vite proxy, so it must know
# the backend address directly.
#
# VITE_API_BASE_URL=https://expensify-backend.domain.com
